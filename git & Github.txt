
git help for commands
git help -a for help in commands 
q for exit
to confirgure   $git config -global user.name "name"
to confirgure   $git config -global user.email "emailid"
to initialize empty git repository   $ git init
 to check files status   $ git status
to add   $git add file name
$ dir to check files in folder
to commit     $ git commit -m "Message to display"
then check status with status command
make changes in file
 then checka status 
 then add using add command to add the modified changes
 then commit
 to creat new file   $ touch new file name with extension
 to add changes $ git add -a
to check content inside the file $ git cat file name
to check logs  $git log
to check previous commit  $ git log -p
$ git diff to check if any updations are left
to remove  $ git rm file name
to make untracked  $ git rm cached file name
to check the branches branch  $ git branch(copy of the master)
to create a branch $ git branch Newbranch
to switch to branch  $ git checkout Newbranch  then add files in newbranch by adding files using touch the add and commit then check status
to switch to master branch  $ git checkout master
to delete the branch  $ git branch -D branchname
to merge the branch files to master branch $ git merge branchname



Git hub

once repo is created in git hub take the folder
$ git init
$ git add .
$ git status 
$ git commit -m "comment you want to give"
$ git remote add origin URLpaste
$ git push -u origin master
